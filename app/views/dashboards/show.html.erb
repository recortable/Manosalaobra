<%= render 'proposed_names/summary' %>
<div class="row">
  <div class="main span6">
    <h1><%= page.title %></h1>
    <%= render_body page %>
    <div class="actions">
        <%= link_to 'Editar', edit_page_path(page) if can? :edit, page %>
    </div>
  </div>
  <div class="sidebar sidebar-right span4 offset2">
    <%= render 'posts/summary' %>
    <%= render 'contact_social_info' %>
  </div>
</div>
